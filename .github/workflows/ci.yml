name: STAR CI

on:
  push:
    branches: [main, v2.0-development, v3.0-knowledge]
  pull_request:
    branches: [main, v2.0-development, v3.0-knowledge]

jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
      - name: Validate Python syntax
        run: python -m compileall -q core gui knowledge modules voice database tests tools main.py config.py diagnostico.py
      - name: Validate repository hygiene
        run: python tools/repo_hygiene.py
      - name: Validate project JSON manifests
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          files = [
              Path('config_skin.json'),
              Path('STAR_MANIFEST.json'),
              Path('knowledge/packs/matematica_basica/manifest.json'),
          ]
          heroes = Path('knowledge/packs/heroes/manifest.json')
          if heroes.exists():
              files.append(heroes)
          for path in files:
              json.loads(path.read_text(encoding='utf-8'))
          print('JSON manifests OK')
          PY
