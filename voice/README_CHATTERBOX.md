# STAR Voice Engine — Chatterbox
1. Execute `INSTALAR_CHATTERBOX.bat` (usa Python 3.11 e cria `.voice_venv`).
2. Execute `TESTAR_VOZ_LOCAL.bat`.
3. O áudio principal está em `voice/reference/star_reference.mp3`.
A STAR principal continua no ambiente `.venv`; a voz roda isoladamente em `.voice_venv`.
